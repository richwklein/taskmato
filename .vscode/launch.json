{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Development Server",
      "command": "./node_modules/.bin/vite .",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
