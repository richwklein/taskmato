import { Box } from '@mui/material'

/**
 * StatisticsView Component
 *
 * This statistics view component is the element for the "statistics" route.
 * It displays statistics about previously completed tasks.
 *
 * @returns the rendered StatisticsView component.
 */
export function StatisticsView() {
  return <Box>{'Statistics'}</Box>
}

export default StatisticsView
